.DS_Store
coverage/
dist/
node_modules/
.idea/
*.iml
npm-debug.log
package-lock.json
.vscode
aws-credentials.json
flow-typed

# Claude Code project config (local, not shared)
.claude/

# Root-level markdown files are local working notes (plans, agent docs);
# README is the only tracked one.
/*.md
!/README.md
.trekker/
